fn main() {
    let uint255: u8 = 255; // u8 range is 0 to 255  , means 2^8 = 256
    let int127: i8 = 127; // -128 to 127
    println!("{}", uint255);
    println!("{}", int127);

   
}

// Unsigned integers can only represent positive values, whereas signed integers
//  can represent both positive and negative values.
